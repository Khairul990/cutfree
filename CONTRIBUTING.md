# Contributing to CutFree

ধন্যবাদ! CutFree ইচ্ছাকৃতভাবেই ছোট আর ডিপেন্ডেন্সি-মুক্ত — এই দুই নিয়ম ধরে রাখলে
কন্ট্রিবিউশন নিয়ে খুব খুশি হব।

## গোল্ডেন রুল

1. **কোনো রানটাইম ডিপেন্ডেন্সি নেই।** না npm প্যাকেজ, না CDN, না বিল্ড স্টেপ। শুধু HTML, CSS, vanilla JS।
   (Playwright শুধু টেস্টের জন্য, `devDependencies`-এ থাকা ঠিক আছে।)
2. **সব কাজ ক্লায়েন্ট-সাইডে।** নতুন ফিচারও ব্রাউজারেই চলতে হবে — কোনো ফাইল কোথাও আপলোড হবে না।
3. **বাংলা + ইংরেজি, দুটোই।** নতুন কোনো টেক্সট যোগ করলে `js/i18n.js`-এ দুই ভাষাতেই স্ট্রিং দিন।
4. **ফ্রি থাকবে।** কোনো ট্র্যাকিং, অ্যাড বা পে-ওয়াল স্ক্রিপ্ট PR-এ নেওয়া হবে না।

## ডেভেলপমেন্ট সেটআপ

```bash
git clone <your-fork> && cd cutfree
python3 -m http.server 8080     # → http://localhost:8080
```

কোনো বিল্ড লাগে না — ফাইল সেভ করে ব্রাউজার রিফ্রেশ করলেই হবে।

টেস্ট চালাতে:

```bash
npm i -D playwright && npx playwright install chromium
npm test
```

## PR পাঠানোর আগে

- [ ] `npm test` পাস করেছে (এবং নতুন ফিচারের জন্য টেস্ট যোগ করেছেন)
- [ ] `node --check js/app.js` ও `node --check js/i18n.js` ক্লিন
- [ ] সিঙ্গেল-ফাইল বিল্ড আপডেট করেছেন: `npm run build` (অথবা CI/রিভিউয়ারকে বলেছেন)
- [ ] ফোনের স্ক্রিনেও একবার দেখে নিয়েছেন (উইন্ডো ছোট করে দেখলেই হবে)
- [ ] commit মেসেজ পরিষ্কার, যেমন: `feat: add playback speed control`

## স্টুডিওতে কন্ট্রিবিউশন

`js/studio/`-এর প্রতিটা মডিউল একা দাঁড়াতে পারে — নতুন কিছু যোগ করার সময় খেয়াল রাখুন:

- **নতুন সিন টাইপ:** `engine.js`-এর `drawSceneContent`-এ একটা `case` যোগ করুন এবং
  `director.js`-এর `classify()`/`build()`-এ সেটা বেছে নেওয়ার নিয়ম লিখুন।
  টেক্সট আঁকার আগে অবশ্যই `fitText()` ব্যবহার করুন (নইলে লম্বা বাংলা/ইংরেজি কপি কাটা পড়বে)।
- **নতুন থিম:** `themes.js`-এ একটা এন্ট্রি — প্যালেট + ব্লেন্ড রেসিপি + গ্রেইন/ভিনিয়েট।
- **নতুন মুড:** `themes.js`-এর `MOODS` + `music.js`-এর `PLAN` (আরপ/ড্রাম/প্যাডের মাত্রা)।
- **পারফরম্যান্স:** নতুন কোনো লেয়ার যোগ করলে `perf` ডায়াল ('high'/'balanced'/'fast')
  অনুযায়ী কমিয়ে দেওয়ার অপশন রাখুন, আর `npm run test:studio` চালিয়ে দেখুন
  রেন্ডার রিয়েল-টাইমের চেয়ে দ্রুত থাকছে কি না।
- **নতুন ট্রানজিশন:** `engine.js`-এর `TRANSITIONS` + `drawSceneExit()`-এ একটা `case`.
  ট্রানজিশন শুধু **exit**-এ চলে — দুটো সিন একসাথে ফেড করাবেন না (ওভারল্যাপ ব্লিড হয়)।
- **সাবটাইটেল:** ফরম্যাট বা টাইমিং লজিক `captions.js`-এ রাখুন (নোডেও চলে), আর
  আঁকার কাজ `engine.js`-এর `drawCaptions()`-এ। টাইম কোথাও `0` হতে পারে — truthiness
  দিয়ে চেক করবেন না, `captions.num()` দিয়ে কোয়ার্স করুন (একটা `0` হারিয়ে গেলে
  পুরো SRT-এ `NaN:NaN:NaN` ঢুকে যায়)।
- **ভয়েস:** `voice.js`-এ কিছু যোগ করলে "ভয়েস নেই / ক্যাপচার নেই" পথটা ভাঙবেন না —
  অনুমান করা টাইমিং (estimated) ফেরত দিলেই UI এগিয়ে যায়।
- **রেন্ডারারকে ডিটারমিনিস্টিক রাখুন:** `Math.random()` নয়, `mulberry32(spec.meta.seed)`
  ব্যবহার করুন — নইলে প্রিভিউ আর এক্সপোর্ট আলাদা হবে।
- **নতুন UI স্ট্রিং:** `js/i18n.js`-এ **দুই ভাষাতেই** key দিন, নইলে টোস্টে কাঁচা key দেখাবে
  (টেস্ট সেকশন 4h এটা ধরে ফেলে)। `data-i18n` এলিমেন্টের ভেতরে লাইভ `id` রাখবেন না —
  ভাষা বদলালে `applyStrings()` ভেতরের markup মুছে দেয়।

## আইডিয়া কোথায়?

`README.md`-এর রোডম্যাপ দেখুন, অথবা নতুন আইডিয়া নিয়ে ইস্যু খুলুন।
বাগ রিপোর্টে ব্রাউজার + ফরম্যাট + ধাপগুলো লিখে দিলে দ্রুত ঠিক করা যায়।
